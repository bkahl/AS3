/**	new class, may not be working yet*/package pdh.graphics{		import flash.display.DisplayObject;	import flash.geom.Matrix;		public class Flip	{				public static function horizontal(dO:DisplayObject):void		{			var matrix:Matrix = dO.transform.matrix;			matrix.a=-1;			matrix.tx=dO.width+dO.x;			dO.transform.matrix=matrix;		}						public static function vertical(dO:DisplayObject):void		{			var matrix:Matrix = dO.transform.matrix;			matrix.d=-1;			matrix.ty=dO.height+dO.y;			dO.transform.matrix=matrix;		}  		} // end class	}  // end package